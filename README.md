# Grupo-3-Servidor
Para correr el servidor de forma local: requisitos:

Tener instalado docker
Tener instalado npm/node
comandos:
   ```bash
    cp .env.example .env
    docker-compose up -d
    npm install
    npm start
   ```
